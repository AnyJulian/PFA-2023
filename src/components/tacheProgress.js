// import ProgressBar from 'react-bootstrap/ProgressBar';

// function ProgressBare() {
//   const now = 60;
//   return <ProgressBar now={now} label={`${now}%`} />;
// }

// export default ProgressBare;